services:
  - type: web
    name: my-backend
    env: node
    plan: free
    buildCommand: npm install --legacy-peer-deps
    startCommand: node app.js
    envVars:
      - key: APP_ENV
        value: production
